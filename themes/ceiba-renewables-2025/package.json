{
  "name": "ceiba-renewables-2025",
  "private": true,
  "version": "1.0.0",
  "description": "Build tooling for the Ceiba child theme (SCSS → style.css).",
  "scripts": {
    "build": "sass src/scss/style.scss:style.css --style=expanded --no-source-map",
    "build:min": "sass src/scss/style.scss:style.css --style=compressed --no-source-map",
    "dev": "sass --watch src/scss/style.scss:style.css --style=expanded"
  },
  "devDependencies": {
    "sass": "^1.77.0"
  }
}

